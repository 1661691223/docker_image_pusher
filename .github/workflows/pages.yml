name: Docker Pages

on:
  # 在 Docker 或 SGLang workflow 完成后自动触发
  workflow_run:
    workflows: ["Docker", "Sync SGLang Production Tags"]
    types:
      - completed
  # 手动触发
  workflow_dispatch:
  # push 到 main 分支时触发（修改 images.txt 或 pages 相关文件）
  push:
    branches: [main]
    paths:
      - 'images.txt'
      - 'scripts/**'
      - 'pages/**'
      - 'data/**'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y jq
          pip install cryptography

      - name: Collect version data
        env:
          ALIYUN_REGISTRY: ${{ secrets.ALIYUN_REGISTRY }}
          ALIYUN_NAME_SPACE: ${{ secrets.ALIYUN_NAME_SPACE }}
        run: |
          chmod +x scripts/collect_versions.sh
          bash scripts/collect_versions.sh
          
          # 合并 sglang tags（如果存在）
          if [ -f data/sglang_tags.json ]; then
            echo "Merging sglang tags..."
            python3 -c "
          import json

          with open('data/versions.json', 'r') as f:
              data = json.load(f)

          with open('data/sglang_tags.json', 'r') as f:
              sglang_tags = json.load(f)

          # 去重合并
          seen = {}
          for img in data['images'] + sglang_tags:
              key = (img.get('name'), img.get('namespace', ''), img.get('tag'), img.get('platform', ''))
              seen[key] = img

          data['images'] = list(seen.values())

          with open('data/versions.json', 'w') as f:
              json.dump(data, f, indent=2, ensure_ascii=False)

          print(f'Merged: {len(sglang_tags)} sglang tags, total {len(data[\"images\"])} images')
          "
          fi

      - name: Generate page
        env:
          ALIYUN_REGISTRY: ${{ secrets.ALIYUN_REGISTRY }}
          ALIYUN_NAME_SPACE: ${{ secrets.ALIYUN_NAME_SPACE }}
          PAGE_MASTER_PASSWORD: ${{ secrets.PAGE_MASTER_PASSWORD }}
          PAGE_USERS: ${{ secrets.PAGE_USERS }}
          PAGE_SESSION_DAYS: ${{ secrets.PAGE_SESSION_DAYS }}
        run: |
          chmod +x scripts/generate_page.sh
          bash scripts/generate_page.sh

      - name: Commit version data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/versions.json
          # 只在有变化时提交
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update version data [skip ci]"
            git push
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: pages/

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
